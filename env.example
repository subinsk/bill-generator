# Distribution Method Configuration
# Options: 'even' (adjust all decimal items) or 'single' (adjust one item only)
DISTRIBUTION_METHOD=even

# Preferred Item for Single Adjustment (optional)
# If using 'single' method, specify item name to prefer, otherwise system will auto-select
PREFERRED_ADJUSTMENT_ITEM=

# Next.js Public Environment Variables
NEXT_PUBLIC_DISTRIBUTION_METHOD=even
NEXT_PUBLIC_PREFERRED_ADJUSTMENT_ITEM=
